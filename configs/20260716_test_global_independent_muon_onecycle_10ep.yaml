# Test config for dev/032_hierarchical_heads_test.py -- evaluates the 10-epoch one-cycle run
# (resumed after the 2026-07-16 machine hang mid-epoch-9-validation; see
# configs/20260716_heads_global_independent_muon_onecycle_10ep_resume.yaml) on the global test
# fold (set '0'), for species macro-F1 directly comparable to mini_trainer-only's ~0.896.
version: 1.0

test:
    model_path: data/global/models/20260716-105029-heads-global-independent-muon-onecycle-10ep-effnetv2s-resume/heads-global-independent-muon-onecycle-10ep-effnetv2s-resume.pt
    parquet_path: data/global/0032836-250426092105405_processing_metadata_postprocessed_quality_filtered.parquet
    img_dir: data/global/images
    out_dir: data/global/preds
    test_set: '0'
    eval_name: global-test
    batch_size: 64
    aug_img_size: 460
    img_size: 256
    num_workers: 12
    optimal_threshold: true
