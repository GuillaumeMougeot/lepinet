# Test counterpart to configs/20260721_backbone_fastvit_t12.yaml (Phase C2). model_path is a glob:
# the run timestamp is minted at train start, so it can't be named in advance.
version: 1.0

test:
    model_path: data/global/models/*-backbone-fastvit_t12-h256-5ep-oversample/backbone-fastvit_t12-h256-5ep-oversample.pt
    parquet_path: data/global/0032836-250426092105405_processing_metadata_postprocessed_quality_filtered.parquet
    img_dir: data/global/images
    out_dir: data/global/preds
    test_set: '0'
    eval_name: global-test
    batch_size: 64
    aug_img_size: 460
    img_size: 256
    num_workers: 12
    optimal_threshold: true
