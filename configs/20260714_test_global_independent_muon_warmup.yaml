# Test config for dev/032_hierarchical_heads_test.py -- evaluates the warmup+lighter-aug
# muon run on the global test fold (set '0'), for a species macro-F1 directly comparable
# to mini_trainer-only's ~0.896. Uses the fixed known-species filter (in-vocab test rows only).
version: 1.0

test:
    model_path: data/global/models/20260713-164456-heads-global-independent-muon-warmup-effnetv2s/heads-global-independent-muon-warmup-effnetv2s.pt
    parquet_path: data/global/0032836-250426092105405_processing_metadata_postprocessed_quality_filtered.parquet
    img_dir: data/global/images
    out_dir: data/global/preds
    test_set: '0'
    eval_name: global-test
    batch_size: 64
    aug_img_size: 460
    img_size: 256
    num_workers: 12
    optimal_threshold: true
