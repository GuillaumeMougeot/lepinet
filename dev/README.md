# dev/ — the lab notebook

Numbered scripts and notebooks, append-only and chronological: the number records *when an idea
happened*, so the sequence tells the project's story. They import each other by filename via
`importlib` (module names can't start with a digit), which is why this is a notebook, not a
library — the cleaned-up, reusable version of the current pipeline lives in
[`../src/lepinet/`](../src/lepinet/).

Three kinds of file live here, with very different lifecycles:
- **The current pipeline** (028 / 030 / 032 / 034 / 036) — living code, edited constantly.
- **Experiments & probes** (031 / 037 / 038 / 039) — measure one thing, then frozen.
- **The historical notebook** (000–027) — the earlier explorations and the superseded
  *multilabel* line; kept as the record, not expected to run.

Results and reasoning are in [`../journal/`](../journal/) (per-question write-ups) and
[`../RESULTS.md`](../RESULTS.md) (the run table, generated by 036).

---

## The current pipeline (hierarchical heads × fastai)

The live system: mini_trainer's hierarchical classification heads (species→genus→family) trained
through fastai's loop. Configs in [`../configs/`](../configs/), best recipe documented there.

| script | what it is |
|---|---|
| **028**`_lepi_hierarchical_multihead_v4.py` | Foundation: `gen_df`/`filter_df`/`prepare_df` (metadata parquet → filtered dataframe, cached), `make_dls` (the fastai `DataBlock`; `lowmem=True` builds it over numpy-indexed items to avoid the fork/COW memory blowup — see [[journal 2026-07-ucloud-benchmark-oom]]), the metrics (`LevelMacroF1`, `StreamingF1MultiHead`), and the plain-softmax multihead-v4 trainer this line grew out of. |
| **030**`_hierarchical_heads_benchmark.py` | **The trainer.** Wraps mini_trainer's heads in `MTHeadAdapter` (runs the head in fp32 under mixed precision; handles the autoregressive MRO workaround), the Muon optimizer (`muon_opt_func`), LR schedules (`warmup_cos`/`one_cycle`/`front_loaded` + `fit_resume` for crash recovery), and the callbacks: `GCCallback` (mini_trainer GPU cycle), `NaNGuard`, `HostMemoryGuard` (anon-based OOM guard), `SupervisionContextCallback` (autoregressive teacher forcing). Defaults to **bf16** — fp16 overflows the cosine/autoregressive heads. |
| **032**`_hierarchical_heads_test.py` | Test/inference counterpart to 030: loads a `.pt`, reconstructs the model, runs inference, writes predictions in the `mini_metrics` schema, and computes the report. `model_path` accepts a glob (resolves the run whose timestamp isn't known at config-write time). Threshold-0 = no abstention. |
| **034**`_longtail.py` | Long-tail helpers: square-root oversampling (`sample_weights` → fastai `WeightedDL`) and logit adjustment (`LogitAdjustment`), both opt-in. Oversampling is the project's biggest single lever ([[journal 2026-07-does-longtail-help]]). |
| **036**`_ledger.py` | Reads every run's saved `config.yaml` + `metrics.json` and prints a table of what each run tested and scored (`--chain` = the ladder; `--snapshot` writes the tracked `RESULTS.md`). The only reader of the otherwise write-only run history. |

## The app / compression line (040–044)

Turning the trained checkpoint into something a phone can run offline. Plan and decisions in
[`../journal/2026-07-lepi-app-claude.md`](../journal/2026-07-lepi-app-claude.md); results in
[`../journal/2026-07-lepi-app-compression.md`](../journal/2026-07-lepi-app-compression.md).
These run against a **local image mirror covering 3,696 of 12,041 species**, so their absolute
numbers are optimistic — relative comparisons are the trustworthy part.

| script | what it is |
|---|---|
| **040**`_onnx_export.py` | Checkpoint → ONNX + `taxonomy.json` + `MANIFEST.json`. Rebuilds the model without fastai `DataLoaders`, bakes ImageNet normalization into the graph (input is RGB [0,1]), emits raw logits. Needs `dynamo=True` (the legacy tracer dies on the head's dict-valued `get_extra_state`) and an eager warm-up pass (the head's mask cache is data-dependent). Verifies PyTorch↔ORT parity. |
| **041**`_ort_parity.py` | Does browser-style resizing cost accuracy? Compares four candidate preprocessings against the exact fastai validation pipeline. Answer: only aspect-ratio squashing matters (−1.1 pp); the kernel does not. `--emit-fixture` writes images + expected logits for the app repo's real-browser test. |
| **042**`_marginalize.py` | Proves genus/family are better derived from the species posterior (log-space `scatter_logsumexp`) than read from their own heads. They are, at both levels on both metrics — so those two heads can be deleted. Also measures how often the independent heads contradict each other (1.8%). |
| **043**`_quantize.py` | int8 dynamic PTQ + the accuracy it costs, at every level and through the marginal path. 3.89× for −0.59 pp species macro-F1. Strips `graph.value_info` first (ORT's quantizer trips over the dynamo exporter's shape annotations). |
| **044**`_calibrate.py` | The threshold estimator. Fits a per-level temperature on the validation fold (exactly, via a 96-point grid of `logsumexp(z/T)` accumulated while streaming — the full logit matrix is never retained), then picks precision-targeted thresholds and reports what they achieve on the held-out fold. Emits `calibration.json` + `thresholds.json`. |

**Phase C (retraining)** runs through the existing `dev/030`/`032` on `configs/`, driven by two
detached sweep scripts (`setsid nohup … </dev/null &` so they outlive the session):
`run_c1_bottleneck_sweep.sh` sweeps the classifier bottleneck `hidden` ∈ {512,256,128};
`run_c2_backbone_sweep.sh` sweeps the backbone at the chosen bottleneck. C2's small nets
(fastvit/repvit/mobilenetv4) come from **timm** — `dev/030` gained `resolve_arch` /
`arch_body_features` / `build_backbone_model` so a config's `model_arch_name` can be either a
torchvision callable or a timm string, and `032`/`040` reconstruct either transparently.

## Experiments & probes (frozen after measuring)

| script | what it measured |
|---|---|
| **031**`_framework_climb_race.py` + `_framework_comparison_report.md` | fastai-only vs mini_trainer-only early-convergence race on the global set. |
| **037**`_dl_memory_probe.py` | Per-`num_workers` DataLoader memory, **no GPU**. Isolated-subprocess + PSS (summing RSS across a fork tree double-counts shared pages). The tool that diagnosed the OOM. |
| **038**`_gpu_decode_train.py` | GPU-decode (nvJPEG) training prototype: CPU workers read raw JPEG bytes, GPU decodes/resizes/augments. `--overlap` double-buffers on a side stream. Proved GPU decode is a **memory** win, not speed, for effnetv2s (model-bound). The nvJPEG trick is folded into `src/lepinet`. |
| **039**`_gpu_decode_profile.py` | Per-stage timing + batch-size + overlap sweep that found the model (not decode) is 77% of a batch. |
| **033**`_multihead_v4_test.py` | Test counterpart to 028's plain-softmax multihead-v4. |
| **030**`_mini_trainer_flaws_summary.md` | Findings on mini_trainer issues met while integrating. |

## Data utilities

| script | what it does |
|---|---|
| **027**`_download_subdataset.py` | Download images referenced in a parquet into `data/small/images`. |
| **029**`_dl_global_lepy.py` | Global-dataset image download (returns ok/skip/fail per image). |
| **010**`_ucloud_vm.py` | UCloud VM helper (superseded by [`../ucloud/`](../ucloud/) + the ucloud-api tool). |

## The historical notebook (000–027) — mostly the multilabel era, superseded

The earlier explorations and the **multilabel** production line (`multilabel-global_lepi`) that
predates the hierarchical-heads rewrite (2026-07-07). Kept as the record; paths point at other
machines and it is not expected to run here. Its configs/launchers are in
[`../archive/`](../archive/).

- **000–002** `lepi_mini_{training,predicting,interpreting}.ipynb` — the first end-to-end mini pipeline.
- **003 / 005 / 011 / 014** `lepi_large_prod*` — the large-scale multilabel production line (v1→v3).
- **022 / 024 / 025** `*_v3_multihead`, `*_multihead`, `integration_asger_heads_v2` — multihead grafted onto the multilabel line; the direct ancestor of 028.
- **012** `lepi_test.py` — multilabel test/eval (scientific-name→taxon-key helpers).
- **008 / 009** `ami_pipeline`, `kavi_pipeline_integration` — AMI / Kavi dataset integrations.
- **004 / 006 / 020** `flemming_*` — the Flemming dataset formatting/eval/correction.
- **007 / 018** `ami_pipeline.ipynb`, `test_ami_model.ipynb` — AMI experiments.
- **013** `species_model_metrics_for_bratislava.ipynb` — a metrics report deliverable.
- **015** `why_fastai_is_slow.ipynb` — a perf investigation (fastai overhead).
- **016 / 017** `integrate_asgers_heads*` — first integration of Asger's heads (→ the 022/025 line).
- **019** `test_fastai_resnet50.ipynb` — a resnet50 baseline check.
- **021** `bioclip2.ipynb` — BioCLIP2 exploration.
- **023** `select_subdataset.ipynb` — building the small subset.
- **026** `tmp.ipynb` — scratch.

---

## Conventions

- **Never `uv sync` / `uv run` here** — the venv is hand-managed; those prune it or trigger a
  build. Use `.venv/bin/python` or `source .venv/bin/activate`. See
  [[journal 2026-07-venv-uv-sync-incident]].
- Run a trainer/test with `python dev/030_...py -c configs/<name>.yaml`.
- Numbered scripts import each other via `importlib` and rely on being launched from the repo
  root (so `dev/` is on `sys.path`).
